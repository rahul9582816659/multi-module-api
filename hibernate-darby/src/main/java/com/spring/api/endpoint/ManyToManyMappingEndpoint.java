package com.spring.api.endpoint;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class ManyToManyMappingEndpoint {

    // Course can have many students
    // student can have many courses
}
